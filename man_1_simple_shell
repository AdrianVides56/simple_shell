.\" manpage for RA_Shell. Simple shell project to Holberton School.
.TH RA_Shell 2 "21 November 2020" "1.0" "mini shell man page"
.SH NAME
.B RA_Shell
- RA_Shell it is a little command line interpreter that reads a users input and excute commands.
.SH SYNOPSIS
.  ./rash
.SH DESCRIPTION
RA_Shell it is a simple UNIX command interpreter to Holberton School project. It function is read a command line string on the standar
input and execute. 
.SH OPTIONS
.B If there are no operands RA_shell shall read commands from the standard input. there are no operands and the shell's standard input and standard error are attached to a terminal, 
.the RA_shell is considered  interactive and not interactive. The no interactive mode a command can be entered and then piped into
./rash
. example:

.echo "/bin/ls" | ./rash

.SH OPERANDS
Operands don't shall be supported by RA_shell

.SH INPUT FILES
Input files not supported

.SH SEE ALSO
acces(2), chdir(2), close (2), closedir(2), execve(2), exit(3), _exit(2), fflush(3), fork(2), free(3), getcwd(3), getline(3) 
isatty(3), kill(2), malloc(3), open(2), opendir(3), perror(3), read(2), readdir(3), signal(2), stat(2), lstat(2), fstat(2) 
strtok(3), wait(2), waitpid(2), wait3(2), wait4(2), write(2).

.SH BUGS
No known bugs.
.SH AUTHORS
.B Adrian Vides and Roberth Rondon

